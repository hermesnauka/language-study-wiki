package com.platform.securelearning;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Confirms the full Spring context wires up correctly (H2 in-memory, no live network hit). */
@SpringBootTest
class SecureLearningApplicationTests {

    @Test
    void contextLoads() {
    }
}
